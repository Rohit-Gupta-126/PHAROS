# PHAROS Phase 0 Python dependencies.
#
# torch is provided by the environment (GPU build cu128 in the `pharos` conda
# env); it is intentionally NOT pinned here so CI / other machines can install
# the appropriate CPU or CUDA build themselves, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#
# The rest are standard PyPI wheels.
numpy>=1.24
h5py>=3.8
scikit-learn>=1.3
scipy>=1.10
matplotlib>=3.7
pandas>=2.0
joblib>=1.3
pyyaml>=6.0
pytest>=7.0
# optional, for the ONNX export / later deploy path:
onnx>=1.15
onnxruntime>=1.16
